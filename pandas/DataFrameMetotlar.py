import pandas as pd import numpy as ndata = {        "Column1": [1,2,3,4,5],        "Column2": [10,25,13,45,24],        "Column3": ["abc","bca","ade","cba","dea"]                }df= pd.DataFrame(data)def kareal(x):    return x*xresult = dfresult = df["Column2"].unique() # tekrarlayanları silerresult = df["Column2"].nunique() # number unique 4 tekil cıkarresult = df["Column2"].value_counts() # değerlerden kaç adet oldugunu söylerresult = df["Column1"] *2result = df["Column1"].apply(kareal)#apply ile fonksiyon uygulayabiliriz.result = df["Column3"].apply(len)#apply ile fonksiyon uygulayabiliriz.df["Column4"] = df["Column3"].apply(len)result = df.columnsresult = len(df.columns)result = df.indexresult = df.inforesult = df.sort_values("Column2")result = df.sort_values("Column3",ascending = False)data = {                "Ay" : ["Mayıs","Haziran","Nisan","Kasım","Aralık","Ocak"],        "Kategori" : ["Elektronik","Elektronik","Elektronik","Kitap","Kitap","Kitap"],        "Gelir" : [20,30,15,14,32,42]        }df = pd.DataFrame(data)print(df.pivot_table(index="Ay",columns= "Kategori",values="Gelir"))print(data)print(result)